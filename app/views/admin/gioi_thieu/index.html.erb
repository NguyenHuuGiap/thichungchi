<div class="row">
  <div class="col-lg-12">
    <h1 class="page-header">Giới Thiệu</h1>
  </div>
  <div id="flash-message">
    <%= render "admin/shared/flash", flash: flash %>
  </div>
</div>
<div class="panel-body">
  <% if @gioithieu.blank? %>
    <a href="<%= new_admin_post_path pattern: "GioiThieu" %>" class="btn btn-success mg-bot-15" role="button">Tạo Giới Thiệu</a>
  <% end %>
  <table width="100%" class="table table-striped table-bordered table-hover" id="dataTables-allpage">
    <thead>
      <tr>
        <th>Title</th>
        <th>Content</th>
        <th>Image</th>
        <th>Created at</th>
        <th>Updated at</th>
        <th>Category</th>
        <th>Action</th>
      </tr>
    </thead>
    <tbody class="data-tbody">
      <% if @gioithieu.present? %>
        <%= render "datatable", post: @gioithieu %>
      <% end %>
    </tbody>
  </table>
</div>
<div id="myModal" class="modal fade z-index-min" role="dialog">
</div>
